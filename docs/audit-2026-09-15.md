# Audit toàn hệ thống — 2026-09-15

Câu hỏi audit: mỗi workflow có nói rõ **lịch chạy · logic · dữ liệu đọc/ghi · success metric (lưu ở đâu) · edge case** không? Rule nào enforce ở đâu?

## Trước audit
| Tiêu chí | 15 skill | Ghi chú |
|---|---|---|
| Lịch/trigger ghi trong skill | 6 | rải rác trong text |
| Đọc/ghi liệt kê trong skill | 0 | chỉ có ở PLAN §C |
| Success metric có tên + target | 0 | 6 mốc chung trong sublet-report |
| Metric **lưu vào DB** | không | tính lại mỗi lần, không có xu hướng |
| Edge case có registry | không | 10 case phân loại (§12) + 100 test; edge case vận hành không ghi đâu |
| Rule → điểm enforce → cách kiểm | không | 19 rule trong CLAUDE.md, 4 gate trong run_skill.sh, không ánh xạ |

## Sau audit (đã làm)
| Việc | File |
|---|---|
| Khối **Spec** chuẩn (Lịch · Trigger · Đọc · Ghi · Metrics · Edge cases · Rules) đầu **14/15 skill** (trừ `information` của Codex) | `.claude/skills/*/SKILL.md` |
| Registry **24 rule** R01–R24: enforce ở đâu, kiểm bằng gì | `docs/rules.md` |
| Registry **~75 edge case** E01–E105 theo 9 stage, kèm skill xử lý + test coverage | `docs/edge-cases.md` |
| Catalogue **35 metric** theo 10 workflow, công thức, target, skill tạo dữ liệu; 5 quyết định tự động | `docs/metrics.md` |
| Bảng `sublet_metrics` (1 dòng/ngày/metric, unique) — đã apply | `db/schema.sql` |
| `report.py --metrics-json` + `sublet-report` insert metrics + kiểm ngưỡng → `sublet_inbox(action)` | `scripts/report.py`, skill |
| Rule mới R20 (flock — cron không chồng), R21 (DB lỗi retry 1 rồi dừng), R23 (không thu phí seeker), R24 (secret) | `CLAUDE.md`, `ops/run_skill.sh` |
| Edge case chưa có handler → thêm: E12 (no permalink), E20 (run bỏ dở), E51 (YES = consent), E83 (subletter đòi thu phí seeker), E84 (found someone sau viewing), E85 (YES trễ), E93 (fee disputed sau 7 ngày), E104 (backup thiếu), E103 (TZ) | scan, inbox-triage, followup, run_skill |

## Còn hở (biết, chưa làm)
| # | Lỗ | Vì sao chưa | Khi nào |
|---|---|---|---|
| 1 | Edge case nhóm D–I (demand/match/outreach/inbox/viewing/fee/ops) **chưa có test tự động** — chỉ có registry + handler trong skill | cần dữ liệu thật để viết kịch bản đúng | sau 10 deal đầu: `tests/ops_cases.md` |
| 2 | Metric `ops.token_cost_eur_24h` chưa có nguồn (skill chưa ghi tokens vào events) | runtime khác nhau (Claude/Codex) báo token khác nhau | backlog J3 #5 |
| 3 | `analyze.qa_kind_acc` nhập tay từ QA tuần | đúng bản chất (người chấm) | giữ |
| 4 | R07 (không headless) không đo được tự động | policy, audit tay | giữ |
| 5 | `information` skill (Codex) không có Spec | Codex sở hữu file | Codex thêm |
| 6 | Enum `check` → Postgres enum | không cấp bách | Phase 1 |

## Cách đọc hệ thống sau audit (cho agent mới)
1. `CLAUDE.md` luật → `docs/rules.md` biết luật enforce ở đâu
2. `PLAN.md` §B–D kiến trúc + state machine → §J chi phí/backlog
3. Mỗi skill: đọc **Spec** trước (7 dòng) là biết nó chạy khi nào, chạm bảng nào, đo bằng gì, phải chịu edge case nào
4. `docs/edge-cases.md` khi gặp tình huống lạ → có mã E thì làm theo; không có → thêm dòng + handler + (nếu được) test
5. `docs/metrics.md` + bảng `sublet_metrics` → biết hệ thống có đang chạy đúng không mà không cần đọc log
